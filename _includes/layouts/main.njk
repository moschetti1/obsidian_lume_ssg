<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/pico.min.css">
    <link rel="stylesheet" href="/css/vars.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <title>{{title}}</title>
</head>
<body>

    <div class="container">
        <div class="row" style="margin-top:50px">

            <!--Profile & nav -->
            <div class="col-md-3 col-sm-12">
                <aside class="profile">
                    <a href="/">
                        <figure class="avatar">
                        <img src="{{profile.avatar}}" alt="">
                    </figure>
                    </a>
                    <hgroup>
                        <h5>{{profile.name}}</h5>
                        <p style="font-size:16px">{{profile.bio}}</p>

                        <div class="links">
                            {%if profile.x%}<a href="{{profile.x}}" target="_blank"><i class="bi bi-twitter-x"></i></a>{%endif%}
                            {%if profile.instagram%}<a href="{{profile.instagram}}" target="_blank"><i class="bi bi-instagram"></i></a>{%endif%}
                            {%if profile.threads%}<a href="{{profile.threads}}" target="_blank"><i class="bi bi-threads"></i></a>{%endif%}
                            {%if profile.youtube%}<a href="{{profile.youtube}}" target="_blank"><i class="bi bi-youtube"></i></a>{%endif%}
                            {%if profile.github%}<a href="{{profile.github}}" target="_blank"><i class="bi bi-github"></i></a>{%endif%}
                        </div>
                    </hgroup>

                    {% include "templates/menu.njk" %}


                </aside>
            </div>

            <!--Content-->
            <div class="col-md-8 col-sm-12">
                {{content | safe}}
            </div>
            
        </div>    
    </div>   


</body>
</html>